# Claude Code. Per-user state (settings, settings.local) stays out, but
# `skills/` is project tooling: it documents this repo's own commands and
# hazards, so it should version with them and show up in review.
#
# Written as `.claude/*` plus a negation rather than `.claude/` because git
# will not descend into an excluded directory, so a re-include under one has
# no effect.
.claude/*
!.claude/skills/

.venv/
__pycache__/
logs/
.env.runtime

# Comic text, never committed. This repo is public, and a prelim groups file
# holds the verbatim dialogue of an in-copyright Barks story -- the same
# material the OCR corpus deliberately keeps in a private repo. Ignored by
# pattern rather than by name so a stray copy cannot slip in under `git add .`.
*-groups.json
ocr-groups-example.json

# Third-party tool download, not a dependency of anything here.
epubcheck-*.zip

# PyCharm / IntelliJ
.idea/**/workspace.xml
.idea/**/tasks.xml
.idea/**/usage.statistics.xml
.idea/**/shelf
.idea/**/aws.xml
.idea/**/contentModel.xml
.idea/**/dataSources/
.idea/**/dataSources.ids
.idea/**/dataSources.local.xml
.idea/**/sqlDataSources.xml
.idea/**/dynamic.xml
.idea/**/uiDesigner.xml
.idea/**/dbnavigator.xml
.idea/**/gradle.xml
.idea/**/libraries
.idea/**/mongoSettings.xml
.idea/**/sonarlint/
.idea/**/sonarIssues.xml
.idea/**/markdown-navigator.xml
.idea/**/markdown-navigator-enh.xml
.idea/**/markdown-navigator/
.idea/$CACHE_FILE$
.idea/codestream.xml
.idea/**/azureSettings.xml
.idea/httpRequests
.idea/caches/build_file_checksums.ser
.idea/replstate.xml
.idea_modules/

# Per-user IDE state, not project config: a plugin identifier tied to this
# install, per-user editor preferences, and the user-scoped spellcheck
# dictionary (the shared one is .idea/dictionaries/project.xml).
.idea/material_theme_project_new.xml
.idea/csv-plugin.xml
.idea/csv-editor.xml
.idea/dictionaries/greg.xml
*.iws
out/
