# From Burbank to Calisota — chapter sidecar for EPUB build.
# Each chapter becomes one entry in the e-reader navigation TOC and must
# specify exactly one anchor:
#   * 'start_printed_page' — must match a printed page number detected on a
#     page (see the Available printed pages list in any build error).
#   * 'start_heading' — matched against a heading item's visible text
#     (case-insensitive).
#
# Note: 'kind = "contents"' is only for books that contain an actual printed
# Table-of-Contents page in their body; this single-article book has none, so
# the navigation TOC is built from the chapters below.

[[chapters]]
title = "Carl Barks — from Burbank to Calisota"
start_printed_page = "37"
